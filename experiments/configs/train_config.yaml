experiment_name: "pcam_mlp_baseline"
seed: 42

data:
  dataset_type: "pcam"
  # TODO: download the dataset and set the relative path here
  data_path: "/scratch-shared/scur2385/surfdrive" 
  input_shape: [3, 96, 96]
  batch_size: 64
  num_workers: 2

model:
  hidden_units: [64, 32]
  dropout_rate: 0.2
  num_classes: 2

training:
  epochs: 3
  log_every: 50
  learning_rate: 0.001
  save_dir: "./experiments/results"
